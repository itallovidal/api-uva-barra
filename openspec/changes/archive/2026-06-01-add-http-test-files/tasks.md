## 1. Healthcheck

- [x] 1.1 Create `http/healthcheck.http` with GET /health endpoint

## 2. Categories

- [x] 2.1 Create `http/category.http` with all 5 category endpoints (create, list, get by ID, update, delete)

## 3. News

- [x] 3.1 Create `http/news.http` with all 6 news endpoints (create, get by ID, update, delete, latest, latest by category)

## 4. Registration

- [x] 4.1 Create `http/registration.http` with all 4 registration endpoints (create request, list requests, approve, reject)

## 5. User

- [x] 5.1 Update `http/user.http` to include all 6 user endpoints (login, create, get by ID, get by email, update, delete)
