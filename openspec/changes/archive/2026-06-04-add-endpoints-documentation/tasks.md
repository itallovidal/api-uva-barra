## 1. Estrutura

- [x] 1.1 Criar diretório `_docs/endpoints/`

## 2. Healthcheck

- [x] 2.1 Criar `_docs/endpoints/healthcheck.md` com documentacão de `GET /health`

## 3. Category

- [x] 3.1 Criar `_docs/endpoints/category.md` com documentacão de `POST /categories`, `GET /categories`, `GET /categories/:id`, `PUT /categories/:id`, `DELETE /categories/:id`

## 4. News

- [x] 4.1 Criar `_docs/endpoints/news.md` com documentacão de `POST /news`, `GET /news/:id`, `GET /news/slug/:slug`, `PUT /news/:id`, `DELETE /news/:id`, `GET /news/latest`, `GET /news/latest/:category`

## 5. Registration

- [x] 5.1 Criar `_docs/endpoints/registration.md` com documentacão de `POST /registration/`, `GET /registration/requests`, `POST /registration/:id/approve`, `POST /registration/:id/reject`

## 6. User

- [x] 6.1 Criar `_docs/endpoints/user.md` com documentacão de `POST /user/login`, `POST /user/`, `GET /user/:id`, `GET /user/email/:email`, `PUT /user/:id`, `DELETE /user/:id`
